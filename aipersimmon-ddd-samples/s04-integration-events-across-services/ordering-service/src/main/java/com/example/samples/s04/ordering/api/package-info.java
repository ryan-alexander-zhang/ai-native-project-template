/**
 * The published language: the events other deployables are allowed to depend on.
 *
 * <p>Two of them, on purpose — one externalized and one not — because "which facts leave this
 * service" is the first question S4 has to answer, and the difference is one annotation.
 */
package com.example.samples.s04.ordering.api;

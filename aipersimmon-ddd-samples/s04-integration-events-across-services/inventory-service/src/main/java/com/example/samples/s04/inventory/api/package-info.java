/**
 * The contracts this service consumes, modelled on its own terms. A consumed contract lives here
 * rather than arriving in a jar from the producer: the wire identity is {@code (name, version)}, so
 * agreement costs nothing and coupling costs a coordinated release.
 */
package com.example.samples.s04.inventory.api;

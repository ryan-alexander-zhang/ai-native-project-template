/**
 * The order's write model. Small on purpose: this sample is about the read side, and the aggregate
 * exists here to produce rows and to move one out of a filtered list mid-pagination.
 */
package com.example.samples.s20.ordering.domain;

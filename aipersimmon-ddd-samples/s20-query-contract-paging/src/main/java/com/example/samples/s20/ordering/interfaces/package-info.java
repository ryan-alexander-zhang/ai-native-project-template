/**
 * The HTTP edge. Two list endpoints, because the two list shapes cost different amounts, and a
 * read-side adapter that does nothing but bind parameters to values.
 */
package com.example.samples.s20.ordering.interfaces;

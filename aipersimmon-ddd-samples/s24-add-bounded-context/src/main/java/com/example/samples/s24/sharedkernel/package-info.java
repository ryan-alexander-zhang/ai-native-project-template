/**
 * The shared kernel's outer package, which holds nothing.
 *
 * <p>Everything is in {@code api} — see that package's documentation for why, and for what does not belong in a
 * shared kernel at all.
 */
package com.example.samples.s24.sharedkernel;

/**
 * The inventory context, which the new one never touched.
 *
 * <p>No dependency in either direction, no change to any of its files. Worth being able to state, because "we added a
 * context and had to touch everything" is the symptom of boundaries drawn along technical layers rather than along
 * language.
 */
package com.example.samples.s24.inventory;

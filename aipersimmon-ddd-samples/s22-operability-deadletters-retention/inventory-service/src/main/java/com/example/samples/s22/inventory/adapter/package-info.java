/**
 * The inbound adapter. Integration-event subscribers live here because an integration event arrives over
 * a transport at the boundary — a library ArchUnit rule says so, and the rule is enforcing the reason.
 */
package com.example.samples.s22.inventory.adapter;

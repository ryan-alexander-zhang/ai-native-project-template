/**
 * The published contract. One event, and the thing S22 says about it: a contract is also an
 * operational commitment, because every dead letter an operator will ever triage is an instance of
 * one of these — identified on the wire by {@code (name, version)}, never by a Java class name.
 */
package com.example.samples.s22.ordering.api;

/**
 * The ordering context, and three tables it is important not to confuse: the write model it owns, a replica of
 * the catalogue's names it has chosen to hold, and a projection derived from both.
 */
package com.example.samples.s12.ordering;

/**
 * Inbound adapters. An integration event arrives over a transport at the boundary, so its subscriber lives
 * here rather than in the application layer — which is what the library's ArchUnit rule enforces.
 */
package com.example.samples.s12.ordering.adapter;

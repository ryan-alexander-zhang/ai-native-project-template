/**
 * The HTTP edge. Its own versioning is a different problem with a different answer: an HTTP client can
 * be told to move, while a record already on a topic cannot.
 */
package com.example.samples.s21.ordering.interfaces;

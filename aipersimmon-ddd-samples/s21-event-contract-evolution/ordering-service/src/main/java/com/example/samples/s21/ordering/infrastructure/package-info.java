/**
 * The adapters. Nothing here mentions a revision: the payload and its version are serialized by the
 * framework's outbox writer, which is why a backlog row keeps the revision it was written at.
 */
package com.example.samples.s21.ordering.infrastructure;

/**
 * Three entry adapters over one application: an HTTP request, the passage of time, and an operator.
 * None of them holds a rule, and all of them reach the domain through the command bus.
 */
package com.example.samples.s11.ordering.interfaces;

/**
 * The order's write model. Its transition table is what makes a scheduled sweep safe: the scan only
 * proposes candidates, and this is what decides.
 */
package com.example.samples.s11.ordering.domain;

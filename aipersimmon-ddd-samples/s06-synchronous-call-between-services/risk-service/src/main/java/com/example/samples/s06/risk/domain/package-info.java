/**
 * The risk context's model: one domain service and one value object, and no aggregate at all.
 *
 * <p>That absence is the honest shape of a decision service. Inventing an aggregate to have one would
 * have produced a class with no invariant to protect and no state to own.
 */
package com.example.samples.s06.risk.domain;

/**
 * The order's write model. It contains no reference to the risk service, by rule: a domain that makes a
 * network call has tied its invariants to somebody else's uptime.
 */
package com.example.samples.s06.ordering.domain;

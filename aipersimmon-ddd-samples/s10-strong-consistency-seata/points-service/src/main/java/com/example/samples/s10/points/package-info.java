/**
 * The points service: one aggregate, exposed twice — once as an AT branch and once as a TCC participant —
 * so the two protocols can be compared against the same model rather than against two different examples.
 */
package com.example.samples.s10.points;

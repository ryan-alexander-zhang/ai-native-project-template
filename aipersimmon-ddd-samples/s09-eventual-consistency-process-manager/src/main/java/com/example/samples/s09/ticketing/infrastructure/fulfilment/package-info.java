/**
 * The two adapters that connect the pure flow to the durable engine: the runtime port implementation and
 * the serialization catalog. Both are infrastructure by the same test — replace the coordinator and these
 * two classes are what you rewrite, while the definition, the inputs and the state are what you keep.
 */
package com.example.samples.s09.ticketing.infrastructure.fulfilment;

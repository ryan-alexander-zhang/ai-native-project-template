/**
 * The persistence adapters for the three aggregates, plus (one package deeper) the two that connect the
 * flow to the durable engine. Nothing here knows the flow's steps.
 */
package com.example.samples.s09.ticketing.infrastructure;

/**
 * Two surfaces, and the split is the sample's answer to who holds the truth: {@code /orders} answers from
 * the aggregate and never mentions a step, {@code /flows} answers from the coordinator and is for whoever
 * is on call.
 */
package com.example.samples.s09.ticketing.adapter;

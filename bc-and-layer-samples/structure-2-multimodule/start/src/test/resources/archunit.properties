# Only analyze our own classes; do not read external/JDK class files (avoids
# ASM parse errors on newer bytecode and keeps the import fast). The package
# rules only need the dependency references from our classes, not the target
# bytecode.
resolveMissingDependenciesFromClassPath=false

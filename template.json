{
  "exclude": [".codex/", ".understand-anything/", "docs/*/[^A-Z]*"],
  "vars": {
    "PROJECT_NAME": { "prompt": "Project name", "default": "{{name}}" }
  },
  "substitute": [],
  "post_create": [
    { "cmd": "git init -q" },
    { "cmd": "git config core.hooksPath .githooks" }
  ]
}

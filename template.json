{
  "exclude": [".codex/", "docs/decision/decision-*.md", "docs/operation/operation-*.md", "bc-and-layer-samples/"],
  "vars": {
    "PROJECT_NAME": { "prompt": "Project name", "default": "{{name}}" }
  },
  "substitute": [],
  "post_create": [
    { "cmd": "git init -q" },
    { "cmd": "git config core.hooksPath .githooks" }
  ]
}

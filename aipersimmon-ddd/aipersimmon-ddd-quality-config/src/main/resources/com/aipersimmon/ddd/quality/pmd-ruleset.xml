<?xml version="1.0"?>
<ruleset name="aipersimmon-ddd-quality"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

  <description>
    AiPersimmon DDD complexity and duplication rules (design-00007). Focused on the
    signals that matter for a framework-free building-block library: cognitive and
    cyclomatic complexity, NPath, method/class size, and parameter lists. Kept narrow
    on purpose — style/formatting is owned by Spotless (google-java-format), architecture
    by ArchUnit, bytecode defects by SpotBugs. Thresholds start permissive and are
    ratcheted per plan-00009.
  </description>

  <!--
    Complexity policy (plan-00009 ratchet). CognitiveComplexity is kept at its default (15) as
    the real readability guard. CyclomaticComplexity / NPathComplexity / ExcessiveParameterList
    are raised well above their defaults on purpose: in this codebase their high counts come from
    patterns that are correct, not tangled — flat guard-clause validation (each `if…throw`
    inflates cyclomatic/NPath) and DI-heavy constructors/@Bean factories (many injected
    collaborators, not data). Gating on those would push toward artificial splitting; genuine
    difficulty is still caught by CognitiveComplexity. TooManyMethods is raised to fit the
    per-item classes here (config @Bean factories, DAO-per-query stores, one-@ExceptionHandler
    -per-exception, codec/state-machine handlers).
  -->
  <rule ref="category/java/design.xml/CognitiveComplexity"/>
  <rule ref="category/java/design.xml/CyclomaticComplexity">
    <properties>
      <property name="methodReportLevel" value="18"/>
    </properties>
  </rule>
  <rule ref="category/java/design.xml/NPathComplexity">
    <properties>
      <property name="reportLevel" value="500"/>
    </properties>
  </rule>
  <rule ref="category/java/design.xml/NcssCount"/>
  <rule ref="category/java/design.xml/ExcessiveParameterList">
    <properties>
      <property name="minimum" value="18"/>
    </properties>
  </rule>
  <rule ref="category/java/design.xml/GodClass"/>
  <rule ref="category/java/design.xml/TooManyMethods">
    <properties>
      <property name="maxmethods" value="30"/>
    </properties>
  </rule>

</ruleset>

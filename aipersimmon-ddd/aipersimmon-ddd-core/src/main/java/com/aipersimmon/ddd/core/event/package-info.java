/**
 * The {@link com.aipersimmon.ddd.core.event.DomainEvent} marker for in-process domain events —
 * facts that have happened within a bounded context, expressed in its ubiquitous language. A domain
 * event is an internal detail of the context, distinct from the versioned contract published to
 * other contexts.
 */
package com.aipersimmon.ddd.core.event;

# Tests reuse the shipped web-store Flyway migration as the single source of the schema, instead
# of a separate test-only schema.sql.
spring.sql.init.schema-locations=classpath:aipersimmon/db/migration/web-store/h2/V1__aipersimmon_web_store.sql

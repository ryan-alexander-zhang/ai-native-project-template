# Tests reuse the shipped saga Flyway migration as the single source of the schema, instead of a
# separate test-only schema.sql.
spring.sql.init.schema-locations=classpath:aipersimmon/db/migration/saga/h2/V1__aipersimmon_saga.sql

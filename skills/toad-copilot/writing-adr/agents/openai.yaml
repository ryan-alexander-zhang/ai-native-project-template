interface:
  display_name: "Decision Record"
  short_description: "Write a decision record in docs/decisions"
  default_prompt: "Use $writing-adr to save this request in docs/decisions as the standard decision record."

policy:
  allow_implicit_invocation: true

interface:
  display_name: "Spec"
  short_description: "Write an engineering spec in docs/spec"
  default_prompt: "Use $writing-spec to turn this into an engineering spec in docs/spec with the right parent, front matter, and design-review flow."

policy:
  allow_implicit_invocation: true

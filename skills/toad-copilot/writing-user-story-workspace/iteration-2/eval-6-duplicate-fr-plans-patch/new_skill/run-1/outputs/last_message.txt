Execution plan:
1. Validate the requested selection against `prd-00020-checkout-redesign`.
   verify: the planner confirms `FR-01` exists in the PRD.
2. Use the planning pass result to choose the story role before generation.
   verify: `FR-01 -> patch`.
3. If you confirm, generate one new user story for `FR-01` with the planned role.
   verify: a new file appears under `docs/user-story` with `parent: prd-00020-checkout-redesign`, `function_requirement_id: FR-01`, and `status: draft`.

Planned role: `patch`

Reason: there is already an existing user story for `prd-00020-checkout-redesign` + `FR-01`, so the planner treats the new one as a duplicate and assigns `role: patch`.

No files have been created yet. Confirm if you want me to generate the user story.

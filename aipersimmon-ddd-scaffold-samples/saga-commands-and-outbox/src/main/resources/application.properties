# Deliver outbox events in process (mode 2): the relay republishes each stored
# event to local @EventListener handlers, which is how the saga's reactions arrive.
aipersimmon.ddd.outbox.dispatch=in-process
aipersimmon.ddd.outbox.poll-delay-ms=200

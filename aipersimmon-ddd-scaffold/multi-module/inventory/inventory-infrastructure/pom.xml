<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.example</groupId>
    <artifactId>multi-module</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <relativePath>../../pom.xml</relativePath>
  </parent>

  <!-- Infrastructure layer of the inventory context: in-memory stock and the event transport. -->
  <artifactId>inventory-infrastructure</artifactId>
  <name>inventory-infrastructure</name>
  <description>Technical port implementations of the inventory context (in-memory).</description>

  <dependencies>
    <!-- DomainEvents: the repository drains the aggregate's recorded events as part of save,
         inside the command transaction, so no handler has to remember a second call (issue-00052). -->
    <dependency>
      <groupId>com.aipersimmon.ddd</groupId>
      <artifactId>aipersimmon-ddd-application</artifactId>
    </dependency>
    <!-- org.springframework.dao.OptimisticLockingFailureException: the repository raises it when a
         version-checked update matches no row, and the framework's ConcurrencyTranslationCommandInterceptor
         turns it into ConcurrencyConflictException -> HTTP 409 (issue-00051). -->
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-tx</artifactId>
    </dependency>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>inventory-domain</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-context</artifactId>
    </dependency>
    <!-- MyBatis-Plus for the JDBC persistence adapters (BaseMapper, wrappers, @TableName). Version
         pinned — the scaffold does not manage MyBatis-Plus (plan-00007). -->
    <dependency>
      <groupId>com.baomidou</groupId>
      <artifactId>mybatis-plus-core</artifactId>
      <version>3.5.15</version>
    </dependency>
  </dependencies>
</project>

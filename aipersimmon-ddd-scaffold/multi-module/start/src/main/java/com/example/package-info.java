/**
 * Bootstrap package: the Spring Boot application that assembles the ordering context's layers into
 * a single deployable. Component scanning from here covers the {@code com.example.ordering} modules
 * on the classpath.
 */
package com.example;

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.example</groupId>
    <artifactId>multi-module</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <relativePath>../../pom.xml</relativePath>
  </parent>

  <!--
    The ordering context's durable Process Manager provider, on the MyBatis-Plus
    (relational) runtime. It owns the order-fulfilment execution code that is specific to
    this provider — the pure ProcessDefinition policy, its state and inputs, the persistence
    codecs, and the runtime-backed implementation of the application's business port.
    The application layer holds only the provider-agnostic contract (the port), so
    swapping this provider (a Temporal/Seata module, a JDBC store) leaves the
    application untouched. Kept a separate module — not a sub-package of application —
    so the process-manager framework dependency is compile-fenced out of the
    application layer (design-00004 §六/§13.1; analysis-00012 §7).
  -->
  <artifactId>ordering-process-mybatis-plus</artifactId>
  <name>ordering-process-mybatis-plus</name>
  <description>The ordering context's order-fulfilment Process Manager provider (MyBatis-Plus runtime).</description>

  <dependencies>
    <!-- The business port this provider implements, plus the ordering commands it dispatches. -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>ordering-application</artifactId>
    </dependency>
    <!-- Evidence-bearing domain types the compensating CancelOrder command carries. -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>ordering-domain</artifactId>
    </dependency>
    <!-- Durable Process Manager contracts: ProcessDefinition, effects, codecs, model. -->
    <dependency>
      <groupId>com.aipersimmon.ddd</groupId>
      <artifactId>aipersimmon-ddd-process-manager</artifactId>
    </dependency>
    <!-- MyBatis-Plus runtime: ProcessRuntime is driven and DefaultProcessQuery resolves the
         instance ref, both over the MyBatis-Plus store beans. -->
    <dependency>
      <groupId>com.aipersimmon.ddd</groupId>
      <artifactId>aipersimmon-ddd-process-manager-mybatis-plus</artifactId>
    </dependency>
    <!-- CQRS contracts: the CommandContext carried on each fact into the runtime. -->
    <dependency>
      <groupId>com.aipersimmon.ddd</groupId>
      <artifactId>aipersimmon-ddd-cqrs</artifactId>
    </dependency>
    <!-- Spring stereotypes: the definition, codecs, and runtime driver are wired beans. -->
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-context</artifactId>
    </dependency>
    <!-- Unit tests for the pure ProcessDefinition transition table; no runtime, no Spring context. -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>

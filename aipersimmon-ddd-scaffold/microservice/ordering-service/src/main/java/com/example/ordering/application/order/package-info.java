/**
 * Order use cases (place, confirm, find) and the ports they need — a repository
 * (defined in the domain) and a domain-event publisher (defined here as a port
 * and implemented by the infrastructure layer).
 */
package com.example.ordering.application.order;

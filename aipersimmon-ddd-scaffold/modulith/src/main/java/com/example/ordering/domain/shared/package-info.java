/**
 * Value objects shared across this bounded context's aggregates, such as
 * {@link com.example.ordering.domain.shared.Money}. Shared within the context
 * only; it is not a cross-context published type.
 */
package com.example.ordering.domain.shared;

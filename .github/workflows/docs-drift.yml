# Doc drift gate: scripts/check-docs-drift.sh. Strict (root-guide placeholders fail) once .ainpt.json marks a generated project.
name: docs-drift

on:
  push:
  pull_request:

jobs:
  drift:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: scripts/check-docs-drift.sh
